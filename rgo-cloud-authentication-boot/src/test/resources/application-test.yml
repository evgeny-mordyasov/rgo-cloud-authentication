server:
  port: 8090

spring:
  main:
    banner-mode: off

module-properties:
  token:
    lifetimeHours: 1
  auth:
    cookie:
      authCookieName: TEST_TOKEN
      path: /
      expirationHours: 1
      secret: test

jasypt:
  encryptor:
    property:
      prefix: STUB(
      suffix: )